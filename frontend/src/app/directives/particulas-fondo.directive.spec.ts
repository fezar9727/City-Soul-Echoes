import { ParticulasFondoDirective } from './particulas-fondo.directive';

describe('ParticulasFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new ParticulasFondoDirective();
    expect(directive).toBeTruthy();
  });
});
